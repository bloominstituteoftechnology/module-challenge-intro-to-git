	https://codepen.io/hryman13/pen/NWGPaxR
	1. What is Semantic HTML? 
		It is computer language that makes it possible for the website to be read by the computer so we can view it.
	2. What is HTML used for? 
		HTML is used for a number of things from blog posting to website design and more.
   	3. What is an attribute and where do we put it? 
		Attributes are characteristics and/or properties of the elements such as width and height of an image. We put them in the quotation marks. 
   	4. What is the h1 tag used for? How many times should I use it on a page?
		h1 tag is used for Main Headers and they are usually only used once per page.
   	5. Name two tags that have required attributes
		Images and Links
   	6. What do we put in the head of our HTML document? 
		<html>
   	7. What is an id? 
		The id is most used to point to a style in a style sheet and can't be overwritten in CSS. They can only be used once per page.
   	8. What elements can I add an id to? 
		Pretty much any save for <base>, <head>, <html>, <meta>, <param>, <script>, <style>, and <title> tag
  	9. How many times can I use the same id on a page? 
		Once per page.
   	10. What is a class? 
		Class is an attribute which specifies one or more items within the HTML.
   	11. What elements can I add a class to? 
		The class attribute can be used on any HTML element.
   	12. How many times can I use the same class on a page? 
		as often as you like.
   	13. How do I get my link to open in a new tab?
		just add a target="_blank" attribute to your links
   	14. What is the alt attribute in the image tag used for? 
		The required alt attribute specifies an alternate text for an image, if the image cannot be displayed
   	15. How do I reference an id?
		<href="">
   	16. What is the difference between a section and a div
		section can be used on paragraphs and div is used as a division of an area.
   	17. What is CSS used for? 
		CSS is used for styling the website 
   	18. How to we select an element? Example - every h2 on the page
		h2{

		}
   	19. What is the difference between a class and an id? - Give me an example of when I might use each one
		class is identified with a . for example .first section
		id is used identifed with # for example #first section
   	20. How do we select classes in CSS?
		.class
   	21. How do we select a p element with a single class of “human””?
		.human p
   	22. What is a parent child selector? When would this be useful? 
		The partent/child selector allows you to select one child in a group of parent/child coding groups and add style to that child that would be different from the others in the same grouping.
		this would be useful when you are working on someone elses work and you are adding a new feature to the site.
   	23. How do you select all links within a div with the class of sidebar?
		div .sidebar it should then select all the links with in that div
   	24. What is a pseudo selector?
		A pseudo-clas is a word added to a selector that adds and extra element of design to it, such as hover. 
   	25. What do we use the change the spacing between lines?
		To change the space between lines you would use the line-height property. This will create the desired spacing between lines thus making it more readable.
   	26. What do we use to change the spacing between letters?
		To change the spacing between letters is to use the letter-spacing property then you can say normal to keep it the normal way or you can change it via px or em.
   	27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
		Changing the text style would be done by using the text-transform property then you state Uppercase, Lowercase, or ALL CAPS to make the desired changes.
   	28. How do I add a 1px border around my div that is dotted and black?
		div{
			border: dotted 1px black
		} 
   	29. How do I select everything on the page? 
		*{

		}
   	30. How do I write a comment in CSS?
		/* */
   	31. How do I find out what file I am in, when I am using the command line?
 		pwd
   	32. Using the command line - how do I see a list of files/folders in my current folder?
		ls
   	33. How do I remove a file via the command line? Why do I have to be careful with this? 
		rm you have to be careful because if you don't really want to remove the item or you remove the wrong one its gone for ever it don't go to recycling bin or trash bin.
   	34. Why should I use version control? 
		Version control helps you when working with codes in a team when you have a live code and you want to add a feature, create updates due to issues, or add new content to the live code. Also if you have several people working one the same code at the same time you would need to know where they are without harming what they are doing or what you are doing. It helps you keep on task and still do your work.
   	35. How often should I commit to github?
		about every 20 minutes. or as often as you feel it would be needed.
   	36. What is the command we would use to push our repo up to github? 
		git push -m "message here"
   	37. Walk me through Lambda's git flow. 
		Go to the project repository
		Make sure you signed in to GitHub
		Press the “Fork” button. It’s near the top of the repository to the right
		Add your TLs as a collaborator. Go to Settings -> Manage Access and click “Invite Collaborator”. You can look up your TLs by their GitHub handles
		Double check that you forked correctly. The Repo should say <yourGitHubHandle>/<projectName> near the top. Then click the green “Clone or Download” button and copy the url. 
		Open up the terminal (Mac) or GitBash (PC) on your machine. 
		Enter the command `git clone <url>` with the url you copied. There is now a local copy of the codebase on your machine. You may enter `ls` and should see the listed. 
		Open that folder in your text editor of choice. 
		Checkout a new branch using `git checkout -b <firstname-lastname>`
		Complete the assignment, adding and committing as needed.
		Push your changes to the remote copy using `git push -u origin <firstname-firatname>`
		Click “New pull request” or the green “Compare & Pull request” button.
		Make sure the base repository is “<yourGitHubHandle>/<projectName>” NOT “LambdaSchool/<projectName>”
		Add your TLs as reviewers.
		Submit the request

Stretch Questions

   	1. What is the difference between an inline element and a block element?
		block level elements are usually structural, while inline elements are usually text based
   	2. What happens when an element is positioned absolutely? 
		An absolutely positioned element is an element whose computed position value is absolute or fixed 
   	3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
		"block formatting context"  give it overflow:hidden
   	4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
		Block by default <div>, <section>  ----- Display inline by default <span>,<b>  ----- inline-block by default  Height or width
   	5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
		