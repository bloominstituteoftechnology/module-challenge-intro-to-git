1. What is Semantic HTML? 


    2. What is HTML used for?
       -Html (Hyper Text Markup Language) is a coding language used to build the Codal structure of a webpage.

    3. What is an attribute and where do we put it? 
       -Attribute helps define charectors/properties of an element's open tag.

    4. What is the h1 tag used for? How many times should I use it on a page?
       -h1 is a tag used to create the title or even focal point of the site (Such as a artist name or Video game title). It CAN be used infinatly but SHOULD only be used once in a code.  

    5. Name two tags that have required attributes
       - <img> (src) or <a> (href).

    6. What do we put in the head of our HTML document? 
       -The title of the wepage and/or anything to do with the heading

    7. What is an id? 
       -An Id is attribute that describes a tag to be further emphasied/stylyized in CSS. Helps make it distinct from the rest of the page. 

    8. What elements can I add an id to? 
       - <p> or <h1> since its a title tag that should be distinct from the rest of the page.

    9. How many times can I use the same id on a page? 
       -technically as much as you want but its generally frowned upon. You should keep each Id disticnt from the last. 

    10. What is a class? 
       -An attribute that allow a devolper to pinpoint a specific html element to be changed in CSS/JS or other programs. 

    11. What elements can I add a class to? 
       - <div> and/or <section>

    12. How many times can I use the same class on a page? 
       - Typically  once.

    13. How do I get my link to open in a new tab?
       - I can create  an <a href>  attribute to connect a link for a website to a link to a img or text. 

    14. What is the alt attribute in the image tag used for?
       - an Alt attribute is used  when a img for whatever cant be seen by the user of a website due to slow connection or an error its source class attribute. 

    15. How do I reference an id?
       -You  first have to make sure that its original class is marked in HTML and then you can use (reference) that id selector in CSS/ JS.

    16. What is the difference between a section and a div
       - A section  is a element used to refer to a particular section in a webpage html code whereas the div blocks elements together to be referred back to in CSS/JS. 

    17. What is CSS used for? 
       -CSS is a coding language used to style a webpage. It uses class selectors to refer back to specific elements in in HTML. 

    18. How to we select an element? Example - every h2 on the page
       - You can select elements using class selectors.

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
       - A class refers to multiple elements but a id only refers to one. A class could be referred with 
    20. How do we select classes in CSS?
    21. How do we select a p element with a single class of “human””?
    22. What is a parent child selector? When would this be useful? 
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector?
    25. What do we use the change the spacing between lines?
    26. What do we use to change the spacing between letters?
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    28. How do I add a 1px border around my div that is dotted and black?
    29. How do I select everything on the page? 
    30. How do I write a comment in CSS?
    31. How do I find out what file I am in, when I am using the command line? 
    32. Using the command line - how do I see a list of files/folders in my current folder?
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
       -  You can remove a file using the command using the rm action but you have to be careful as this is a permanant deletion that cant be retrieved after delete the file. 

    34. Why should I use version control? 
    35. How often should I commit to github?
       -often

    36. What is the command we would use to push our repo up to github? 
       -
    37. Walk me through Lambda's git flow. 
