 CODEPEN EXERCISE:

 https://codepen.io/everybodyneedsis/pen/LYxjeom?editors=1100
 
 
 1. What is Semantic HTML? 
Semantic html is elements that describe it's meaning to the browser and developer //// example: <form> <table>

    2. What is HTML used for? 
The code used to structure a web page and the content

    3. What is an attribute and where do we put it? 
An attribute provides additional information about html elements, they're always specified in the start tag

    4. What is the h1 tag used for? How many times should I use it on a page?
The h1 tag is generally used for the headline of the webpage, should only be used once throughout the entire page

    5. Name two tags that have required attributes
<Img> and <A>

    6. What do we put in the head of our HTML document? 
Head is used for metadata, generally tags like <title> <style> <link> etc

    7. What is an id? 
Id is used to specify a unique id for an html element to help be precise

    8. What elements can I add an id to? 
<h1> <div> <span>

    9. How many times can I use the same id on a page? 
ONCE!

    10. What is a class? 
An attribute to specify a class for an html element

    11. What elements can I add a class to?
<div> <span> 
 
    12. How many times can I use the same class on a page? 
Multiple times!

    13. How do I get my link to open in a new tab?
<a href="https://www.jacobkylefranklin.com" target="_blank">Jacobs Website</a>

    14. What is the alt attribute in the image tag used for? 
It is used to specify alternative text for an image if it does not show up

    15. How do I reference an id?
#ID

    16. What is the difference between a section and a div
<section> content inside of it is grouped together
<div> element has no special meaning, it represents the children

    17. What is CSS used for? 
Styling your html/webpage

    18. How to we select an element? Example - every h2 on the page
H2 {}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
ID is extremely specific and can only be used once on the webpage to define an element. Classes are less specific and can be used throughout your code to call upon multiple things at once, if needed.

<h1 id="myHeader">My Cities</h1>

<h2 class="city">London</h2>
<p>London is the capital of England.</p>

<h2 class="city">Paris</h2>
<p>Paris is the capital of France.</p>

<h2 class="city">Tokyo</h2>
<p>Tokyo is the capital of Japan.</p>

    20. How do we select classes in CSS?
.Classes

    21. How do we select a p element with a single class of “human””?
P.human {}

    22. What is a parent child selector? When would this be useful? 
Selects all elements that are a direct child of the specified element
Useful to use if you're trying to select a child that's nested within a parent element
Example: div > P {}

    23. How do you select all links within a div with the class of sidebar?
div.sidebar a {}

    24. What is a pseudo selector?
Used to define a special state of an element
example:  H1:hover {}

    25. What do we use the change the spacing between lines?
Line-height property

    26. What do we use to change the spacing between letters?
Letter-spacing property

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
Text-transform: capitalize; 
Text-transform: lowercase;

    28. How do I add a 1px border around my div that is dotted and black?
Div {
	border: 1px dotted black;
}

    29. How do I select everything on the page? 
Body {}

    30. How do I write a comment in CSS?
/* this is comment */

    31. How do I find out what file I am in, when I am using the command line? 
$ Pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
$ ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
$ rm
You can accidentally remove your files :(

    34. Why should I use version control? 
Because it helps not mess up the master version while you are making your own edits to the file

    35. How often should I commit to github?
Veryyyy often and use descriptive commit messages

    36. What is the command we would use to push our repo up to github? 
$ git push -u origin branch-name

    37. Walk me through Lambda's git flow. 
Fork your repo, then clone it in terminal
go to the directory
Use $ git checkout -b 'first name-lastname' to command your own branch name
Now you an work on your repo 
Commit any changes to your branch
Push your changes back to your Git repo
Go back to GitHub, find your forked name-branch
Create and submit Pull Request link into Canvas

Stretch Questions

    1. What is the difference between an inline element and a block element?
Block element always starts on new line
Inline elements do not start on new line

    2. What happens when an element is positioned absolutely? 
It is positioned relative to the nearest positioned ancestor

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
inline block

    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
Display:block -- <div> <h1 - h6> <p>
Display:inline -- <span> <a> <img>
Display:inline-block -- <button> <textarea> <input>

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

Box model is essentially a box that wraps around every html element
