1. What is Semantic HTML? 
    2. What is HTML used for? HTML is used to mark up a webpage and is used as a structure for a website. Its like the frame of a house. 
    3. What is an attribute and where do we put it? Attributes are used in opening tags to modify elements and change their behavior.
    4. What is the h1 tag used for? How many times should I use it on a page? As a title or heading for you webpage, you should only ue it once per page.
    5. Name two tags that have required attributes? the <i> and <a> tags both require attributes.s
    6. What do we put in the head of our HTML document? The title, styles, and scripts should go in the heas of an html file. 
    7. What is an id? an id gives a unique ID for an html element and is used to delcare a specific style in CSS.
    8. What elements can I add an id to? and id can be given to any html element.
    9. How many times can I use the same id on a page? every id must be unique and should only be used once on a page.  
    10. What is a class? a class groups an element/elements into a class which is used to point to a class in  astyle sheet or a script.
    11. What elements can I add a class to? All elements can be added to a class.
    12. How many times can I use the same class on a page? as many times as you like. 
    13. How do I get my link to open in a new tab? You would use an <a> tag with href followed by your website in quotes. example: <a href="https://www.facebook.com"</a>
    14. What is the alt attribute in the image tag used for? the alt tag is used as a back up incase there is an issue with the image not being able to load. 
    15. How do I reference an id? with a hastag ie: #
    16. What is the difference between a section and a div? a section definines sections in html like chapters in a book while div is used to group block elements to be styled in css.
    17. What is CSS used for? CSS is used to style html elements .
    18. How to we select an element? Example - every h2 on the page? h2 { }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. id should be used to style an element that appears on the page only once while class is used to style multiple elemetns
	id example would be a <h1> tag. class example would be <p> tags
    20. How do we select classes in CSS? you would use a . then the name of the class. ie: .class
    21. How do we select a p element with a single class of “human””? .human 
    22. What is a parent child selector? When would this be useful?  used to select elements with a specific parent.  div > p would be a good example. It selects all <p> elements within a <div> tag
    23. How do you select all links within a div with the class of sidebar? div > .sidebar 
    24. What is a pseudo selector? Pseudo selecors are CSS classes or elements used to define the state of an element. ig:  .hover will change the style of an element or class when the mouse cursor is hovered over the element.
    25. What do we use the change the spacing between lines? you could use the line height property. ie: line-height: 25px;
    26. What do we use to change the spacing between letters? you could use the letter-spacing property. ie: letter-spacing: 3px;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? you could use the text-transform propterty. ie: text-transform: uppercase;
    28. How do I add a 1px border around my div that is dotted and black? div { border: 1px dotted black ; }
    29. How do I select everything on the page? use an asterisk. ie: *
    30. How do I write a comment in CSS? /* this is a comment */ 
    31. How do I find out what file I am in, when I am using the command line? 'pwd'
    32. Using the command line - how do I see a list of files/folders in my current folder? 'ls'
    33. How do I remove a file via the command line? Why do I have to be careful with this? 'rm' then the path and file name of the file you want to delete. You could seriously mess up your operating system if you are not careful.
    34. Why should I use version control? So you can work on new features and lookback at older versions when needed 
    35. How often should I commit to github? as often as possible 
    36. What is the command we would use to push our repo up to github? git push 'repo-name' 'branch-name'
    37. Walk me through Lambda's git flow. You would fork and clone the repo, creat your own branch, commit your changes, push back to your branch, and request a pull to the main repository 

Stretch Questions

    1. What is the difference between an inline element and a block element? inline elents do not create a new line and only take the amount of width the need while block elements always create a new line and take all available width. 
    2. What happens when an element is positioned absolutely? absolute allows you to put an element in the exact postion you want to on a display. 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? You would use margin and padding around your element.
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default: Block by default: <table> <nav> <li>; inline by default: <a> <span>;  inline-block by default: <button>
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? everything in html has a box around it like bricks on a wall and CSS is used to style those bricks. You could use the width property in CSS on the parent element in your html file. ig: body { widith: 80% }


Codepen URL: https://codepen.io/hoboman88/pen/yLOdqzX