Link to finished Codepen https://codepen.io/TylerJacobson/pen/ZEWEExZ?editors=0100
<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? Html with clear meaning.
    2. What is HTML used for? Creating web pages
    3. What is an attribute and where do we put it? Additional information at the beginning of a tag IE: href=""
    4. What is the h1 tag used for? How many times should I use it on a page? Main header. Once.
    5. Name two tags that have required attributes <img> <a>.
    6. What do we put in the head of our HTML document? Usually a header and navigation menu.
    7. What is an id? An attribute that can be used to identify an element.
    8. What elements can I add an id to? Any tag.
    9. How many times can I use the same id on a page? Once.
    10. What is a class? An attribute that can be used with multiple elements.
    11. What elements can I add a class to? Any tag.
    12. How many times can I use the same class on a page? As many times as needed.
    13. How do I get my link to open in a new tab? target="_blank".
    14. What is the alt attribute in the image tag used for? A description of the image.
    15. How do I reference an id? #idName.
    16. What is the difference between a section and a div? Content inside of a section should be considered grouped.
    17. What is CSS used for? Styling.
    18. How to we select an element? Example - every h2 on the page? h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one Ids can only be used once on a page, so they would mostly be used to select and modify a specific object on the page. Classes can be used multiple times on a page, so they would be used for any widespread modification.
    20. How do we select classes in CSS? .className
    21. How do we select a p element with a single class of “human””? p .human
    22. What is a parent child selector? When would this be useful? A selector used to match only elements that are the direct children of another element. It would be useful to make changes to very specific portions of a page.
    23. How do you select all links within a div with the class of sidebar? .sidebar > a {}.
    24. What is a pseudo selector? Special state selectors such as Hover.
    25. What do we use the change the spacing between lines? line-height.
    26. What do we use to change the spacing between letters? letter-spacing.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? uppercase, lowercase, capitalize.
    28. How do I add a 1px border around my div that is dotted and black? div {border: 1px dotted black;}
    29. How do I select everything on the page? * {}
    30. How do I write a comment in CSS? /* */
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm, the file will not go to recycle bin
    34. Why should I use version control? To safely make changes to your work, while saving previous iterations.
    35. How often should I commit to github? After making a significant change, or every 20 minutes or so.
    36. What is the command we would use to push our repo up to github? push
    37. Walk me through Lambda's git flow. Fork the repo, add TL, clone the forked repo, create a new branch, work on project, push changes, submit pull request.
Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 