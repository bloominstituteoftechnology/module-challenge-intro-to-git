Here is the link to my project: 

https://codepen.io/cainpalmer/pen/xxdJQQv

Here are the questions:

1. What is Semantic HTML? 
Semantic HTML is any code within HTML that others or a computer can identify as being used for a particular purpose.

    2. What is HTML used for? 
HTML is used to markup webpages in a manner that computers can understand.

    3. What is an attribute and where do we put it? 
An attricute is a part of HTML code that provides additional info about elements in the start tag.

    4. What is the h1 tag used for? How many times should I use it on a page?
The H1 tag is used to identify which string is the most important line of text onn a webpage.

    5. Name two tags that have required attributes
The <a> tag and the <img> tags require attributes.

    6. What do we put in the head of our HTML document? 
We put the title of our webpages in the head of our HTML document.

    7. What is an id? 
An ID is a unique identifier that identifies an element when linking, scripting, or styling with CSS.

    8. What elements can I add an id to?
    You can add ID's to elements such as headings.

    9. How many times can I use the same id on a page? 
    You can only use a specific ID once per HTML page.

    10. What is a class? 
    A class in HTML is an attribute used to point to certain class name style sheets.

    11. What elements can I add a class to? 
    You can add classes to elements such as headings, <div>, and many more.

    12. How many times can I use the same class on a page? 
    You can use a specific class as many times as you want per HTML page.

    13. How do I get my link to open in a new tab?
    To get a link to open into a new tab, you would use the <a> element.

    14. What is the alt attribute in the image tag used for? 
    The alt attribute is used to discribe an image that cannot be displayed for refrence.

    15. How do I reference an id?
    You can refrence an ID by using a hashtag beforehand.

    16. What is the difference between a section and a div
    Section groups content, while div only has meaning within certain classes/attributes.

    17. What is CSS used for? 
    CSS is used to design and professionalize webpages.
    
    18. How to we select an element? Example - every h2 on the page
    You would use the general sibling selector to select all labels.

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    A class allows CSS and JS to select and access specific elements in a group while an ID is used to identify elements when linking.
    ex) <h1 id="myHeader"> vs <h2 class="city">

    20. How do we select classes in CSS?
    You can select classes in CSS by putting a period before the class type.

    21. How do we select a p element with a single class of “human””?
    You can select a <p> element and a specific class of "human" by writing: .human p:{ *CSS* }

    22. What is a parent child selector? When would this be useful? 
    A parent child selector is a selector used to select all elements that are the direct child of a parent element, which would be most useful when searching for specifically organized elements in code.

    23. How do you select all links within a div with the class of sidebar?
    To select all links with the class of sidebar, you would use a Query Selector.

    24. What is a pseudo selector?
    A pseudo selector that selects elements that are in a specific state.

    25. What do we use the change the spacing between lines?
    We can change spacing by using a <br> tag.

    26. What do we use to change the spacing between letters?
    We can use the property in CSS called: letter-spacing.

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    We can use the property in CSS called: text-transform.

    28. How do I add a 1px border around my div that is dotted and black?
    We can use the property in CSS called: border-style.

    29. How do I select everything on the page?
    We can select everything on the page in CSS by using the * icon.

    30. How do I write a comment in CSS?
    You can write a comment by using: /* anything here is invisible */

    31. How do I find out what file I am in, when I am using the command line? 
    You can find out what file you're in by using: 'pwd'

    32. Using the command line - how do I see a list of files/folders in my current folder?
    You can see the list of files by using: 'ls'

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    You can remove files with the command line by using: del /f *filename*, althought you must be careful because you can sometimes accidentally delete everything in that directory.

    34. Why should I use version control? 
    Version Controlo should be used to make changes to code and compare versions to prevent bugs from being used in final products.

    35. How often should I commit to github?
    You should commit a lot of time per week to GitHub.

    36. What is the command we would use to push our repo up to github? 
    You would use something like: git push -u origin ___cain-palmer___

    37. Walk me through Lambda's git flow. 
    First, fork the code. Second, clone the code. Third, use 'cd' to find the directory you want to fork to. Forth, use 'git clone ___link___' to clone into the directory. Fifth, use 'cd' to enter the new directory. Sixth, use 'git checkout -b __first-last__' to create a branch. Seventh, work on your branch. Eighth, save changes with 'git add .' and remember to save often. Ninth, push the code back to GitHub using 'git push -u origin __branch-name__' and you are done.

    done.