 Link to codepen: https://codepen.io/JacobPRC/pen/eYNJozG?editors=1100
 
 1. What is Semantic HTML? 
    2. What is HTML used for? To improve readibility and create sections to implement css and js
    3. What is an attribute and where do we put it? it links to a source (usually a link or doc) and is usually added to <a>, <img>, and <script>. ex: <a href="google.com">google</a>
    4. What is the h1 tag used for? How many times should I use it on a page? It is the main heading or title of a page/post. Should only be used once.
    5. Name two tags that have required attributes: <a> and <script>
    6. What do we put in the head of our HTML document? <style>, <base>, <link>, <meta>, <script>, <title>, etc
    7. What is an id? it provides unique id to tag and carries more weight than class
    8. What elements can I add an id to? A little confused by the wording. You can use an id in css or js.
    9. How many times can I use the same id on a page? 1
    10. What is a class? basically the same thing as an id, but it carries less weight in css.
    11. What elements can I add a class to? <div>, <h1, h2, h3, h4, h5, h6>,<section>, etc
    12. How many times can I use the same class on a page? No limit
    13. How do I get my link to open in a new tab? add target="_blank" after href
    14. What is the alt attribute used for? It has some text describing the pic if it can't load properly.
    15. How do I reference an id? in css you do this #new-id{}; in js you write something like this: const id = document.querySelector(#newId);
    16. What is the difference between a section and a div Section contains a large section of text and div divides parts of it. So if 4 paras were in a section, maybe you'd have 2 paras in e/ div
    17. What is CSS used for? Applying visual design to a static page. 
    18. How to we select an element? Example - every h2 on the page h2{color:black;}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. Id carries more weight than class. You could use it if css is very important, or if there are many items w/ the same class, but you just want one of them. You can also use something like: .class-name h2{}
    20. How do we select classes in CSS? .class-name{color:blue;}
    21. How do we select a p element with a single class of “human”? p.human{}
    22. What is a parent child selector? When would this be useful? if you want to change one p tag not all of them. ie .div-class p{}
    23. How do you select all links within a div with the class of sidebar? div.sidebar a{}
    24. What is a pseudo selector? selects a state of an item ie:hover which only applies when you hover over something
    25. What do we use the change the spacing between lines?
    26. What do we use to change the spacing between letters?
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    28. How do I add a 1px border around my div that is dotted and black?
    29. How do I select everything on the page? 
    30. How do I write a comment in CSS?
    31. How do I find out what file I am in, when I am using the command line? 
    32. Using the command line - how do I see a list of files/folders in my current folder?
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    34. Why should I use version control? 
    35. How often should I commit to github?
    36. What is the command we would use to push our repo up to github? 
    37. Walk me through Lambda's git flow