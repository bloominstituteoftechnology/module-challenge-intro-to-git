Link to code pen (part 1): https://codepen.io/mcampbell96_/pen/jOWwwpN
Link to code pen (part 2):https://codepen.io/mcampbell96_/pen/OJMOKdr

<!-- Questions to Submit in today's file first-lastname.txt -->
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes.

    1. What is Semantic HTML?
    > Semantic HTML helps search engines determine the significance and context of the content. 
    2. What is HTML used for?
    >HTML is used for creating web pages. It tells the webpage what to display and how to display it. 
    3. What is an attribute and where do we put it?
    >An attribute is an identifier for a section of the webpage and we put it alongside the element. 
    4. What is the h1 tag used for? How many times should I use it on a page?
    >the h1 tag is used to define a header and should only be used once as it's most important. 
    5. Name two tags that have required attributes
    > img src="" and a href="". Both img and hrefs req
    6. What do we put in the head of our HTML document?
>Usually the title of the document 
    7. What is an id?
    >an attribute that specifies a elements in a HTML document 
    8. What elements can I add an id to?
    >any element can use the id tag
    9. How many times can I use the same id on a page?
    >once
    10. What is a class?
    >an attribute that specifies a elements in a HTML document 
    11. What elements can I add a class to?
    >Any 
    12. How many times can I use the same class on a page?
    >Multiple 
    13. How do I get my link to open in a new tab?
    >Right click on the link and click on "Link In New Tab"
    14. What is the alt attribute in the image tag used for?
> It’s used for accessibility, when the image is not able to be read, the alt will let speak what the image is 
    15. How do I reference an id?
    >In CSS you use #{idname}
    16. What is the difference between a section and a div
> A section is more of a semantic type element and the div is more of a presentation element used for grouping things together. 
    17. What is CSS used for?
    >CSS is used to customize the HMTL page. Think of CSS as coloring pencils to a coloring book.
    18. How to we select an element? Example - every h2 on the page
    > h2{}. You use, elementName{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
> There pretty much the same however the ID can only be used once whereas the class can be used multiple times. Both are used to identify different elements in the HTML 
    20. How do we select classes in CSS?
    >For classes you use a dot. Example: .elementName{}
    21. How do we select a p element with a single class of “human””?
> .human p{} 
    22. What is a parent child selector? When would this be useful?
> the parent child selector is used to customize one child element in the HTML document. It could be used to customize a single element if other child elements are present 
    23. How do you select all links within a div with the class of sidebar?
>.sidebar img {} 
    24. What is a pseudo selector?
> It’s a feature used to specify a special state to an element like the :hover, which allows the button to change color when the user hovers over it.
    25. What do we use the change the spacing between lines?
> You can do line-height 
    26. What do we use to change the spacing between letters?
> For this you can use letter-spacing 
    27. What do we use to change everything to CAPITALS? lowercase? Capitalize?
> You can use supercase, lowercase, or capitalize
    28. How do I add a 1px border around my div that is dotted and black?
> div {border 1px border-style: dotted color: Black}; 
    29. How do I select everything on the page?
> Using the asterisk (*) 
    30. How do I write a comment in CSS?
>/* , and end it with */
    31. How do I find out what file I am in, when I am using the command line?
> You can use pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
> You use `ls` 
    33. How do I remove a file via the command line? Why do I have to be careful with this?
>You can use `rm`
    34. Why should I use version control?
> This allows you to revert and work easily with others on different projects 
    35. How often should I commit to github?
> Best practice is to commit anytime you make changes that you want to save. 
    36. What is the command we would use to push our repo up to github?
> git push 
    37. Walk me through Lambda's git flow.
> Fork the file from Lambda 
> Use git clone (link provided)
> Work on the file, and use git commit -m (add description of what you did) 
> Use git push to push the changes to github 
> Add TL as collaborator 
>Push into the Lambda School folder

