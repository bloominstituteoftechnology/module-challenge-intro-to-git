https://codepen.io/derekjdyer/pen/pooWgBq

    1. What is Semantic HTML? Semantic HTML is writing HTML with tags that induce meaning. i.e. <section></section>

    2. What is HTML used for? HTML is used to organize content on a webpage. 

    3. What is an attribute and where do we put it? An HTML attribute is something that extends an elements behavior. i.e. class or ID

    4. What is the h1 tag used for? How many times should I use it on a page? The <h1></h1> tag is used for the main title of the webpage and should be used once.

    5. Name two tags that have required attributes. The anchor tag and the image tag

    6. What do we put in the head of our HTML document? <h1></h1> and <nav></nav>* if applicable

    7. What is an id? An id is a way to specify an individual HTML element. For example <div id="sampleID"></div>

    8. What elements can I add an id to? It is global so it can be applied to any HTML element.

    9. How many times can I use the same id on a page? Once, they are unique

    10. What is a class? A class is similar to an ID but you can specify one or more class names to an element.

    11. What elements can I add a class to? It is also global and can be applied to any HTML element.

    12. How many times can I use the same class on a page? On more than one element.

    13. How do I get my link to open in a new tab? add target="_blank" to your anchor tag.

    14. What is the alt attribute used for? It provides alternative information in case an image or text cannot be displayed.

    15. How do I reference an id? #idName

    16. What is the difference between a section and a div? A section is semantic and a div is not.

    17. What is CSS used for? Styling HTML documents

    18. How to we select an element? Example - every h2 on the page h2{code;}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one. The difference between an ID and a class is that an ID can be used to identify one element, whereas a class can be used to identify more than one. A class should be used to style more than one element and ID to style a particular element.

    20. How do we select classes in CSS? .className{code;}

    21. How do we select a p element with a single class of “human””? p.human{code;}

    22. What is a parent child selector? When would this be useful? A parent child selector allows you to specify an element that is part of another element and allows you to style it differently

    23. How do you select all links within a div with the class of sidebar? a.sidebar{code;} 

    24. What is a pseudo selector? A psuedo selector either effects the first or last line of an element.

    25. What do we use the change the spacing between lines? line-height

    26. What do we use to change the spacing between letters? letter-spacing

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform

    28. How do I add a 1px border around my div that is dotted and black? border:1px dotted black;

    29. How do I select everything on the page? *{code;}

    30. How do I write a comment in CSS?/*THIS IS A COMMENT*/

    31. How do I find out what file I am in, when I am using the command line? pwd

    32. Using the command line - how do I see a list of files/folders in my current folder? ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? rm <filename> removing files this way are permanently gone and will not go to recycle bin or be recoverable

    34. Why should I use version control? It allows multiple users to work in the same file on their own branch and allows you to revert back to a successful build if there is a problem.

    35. How often should I commit to github? Once every 20 - 30 minutes.

    36. What is the command we would use to push our repo up to github? git push

    37. Walk me through Lambda's git flow. 

    Fork
    Add team lead as a collaborator
    Clone
    create a branch: git checkout -b 'firstName-lastName'
    Submit work with a pull request

Stretch Questions

    1. What is the difference between an inline element and a block element? A block element takes the full width available and starts a new line

    2. What happens when an element is positioned absolutely? It is positioned relative to its nearest ancestor. It scrolls with the page if none exists.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

    5. In your own words, explain the box model. What is the fix for the box model? 