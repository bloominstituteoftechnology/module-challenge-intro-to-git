link : https://codepen.io/Austin-reed96/pen/VwvxgEY




    1.What is Semantic HTML? The process of providing meaning to your content so its easier for computers and humans to read
    2. What is HTML used for? its used to create websites 
    3. What is an attribute and where do we put it? it provides additional information about an element that always starts a tag
    4. What is the h1 tag used for? How many times should I use it on a page? the h1 is used for a header mostly, its the biggest text size, only used once 
    5. Name two tags that have required attributes img and a
    6. What do we put in the head of our HTML document? a header 
    7. What is an id?  unique id for html and msotly used for styling
    8. What elements can I add an id to? all elements 
    9. How many times can I use the same id on a page? once
    10. What is a class? it is more than one element 
    11. What elements can I add a class to? all html elements with the same class
    12. How many times can I use the same class on a page? multiple times
    13. How do I get my link to open in a new tab? set the target attribute to .blank
    14. What is the alt attribute in the image tag used for? its the alternative text if an image doesnt show
    15. How do I reference an id? with a #
    16. What is the difference between a section and a div? a section is where the content inside is grouped and div does only whats in its class
    17. What is CSS used for? styling a page
    18. How to we select an element? Example - every h2 on the page, use use the h2,{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one, an id is you would use an id tag for like a header or footer and a class tag would be for like a <p>
    20. How do we select classes in CSS? with a (.)
    21. How do we select a p element with a single class of “human””?
    22. What is a parent child selector? When would this be useful? the parent element selects all elemnts that are a direct child of the specific element.
    23. How do you select all links within a div with the class of sidebar? use decendant selectors
    24. What is a pseudo selector? selects elements in a specific state 
    25. What do we use the change the spacing between lines? line height
    26. What do we use to change the spacing between letters? letter spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? select everything the command capital or lowercase
    28. How do I add a 1px border around my div that is dotted and black? div {border: dotted 1px black;}
    29. How do I select everything on the page? using *
    30. How do I write a comment in CSS? */
    31. How do I find out what file I am in, when I am using the command line? the tilda will bring you to the top of the file
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm is the command and you have to be careful cause it doesnt warn you and will just delete
    34. Why should I use version control?Version control systems are a category of software tools that help a software team manage changes to source code over time.
    35. How often should I commit to github? whenever you are finished with a task you should
    36. What is the command we would use to push our repo up to github? gitPUSH
    37. Walk me through Lambda's git flow. first you fork the doc, then add your tl, clone the repo, create a branch, create a pull request, and add tl for pull request 

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

