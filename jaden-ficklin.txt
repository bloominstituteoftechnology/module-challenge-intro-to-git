https://codepen.io/JadenFicklin/pen/dyWMYNw

    1. What is Semantic HTML? used to reference different categories of your html document
    2. What is HTML used for? to create a skeleton of your website
    3. What is an attribute and where do we put it? you put it inside of the opening tag and its used to add functions like in <img src="blue.jpg"> src is the attribute.
    4. What is the h1 tag used for? How many times should I use it on a page? used to declare text as a header, the largest of the 6 headers, should be used only once on a page.
    5. Name two tags that have required attributes. script and img
    6. What do we put in the head of our HTML document? <head> <meta charset="UTF-8"> <link rel="stylesheet" href="style.css"> </head>
    7. What is an id? classifies a name for whichever tag you need, can only have 1 because javascript will go crazy if there is more than one
    8. What elements can I add an id to? any elements
    9. How many times can I use the same id on a page? once
    10. What is a class? classifies a name for multiple tags
    11. What elements can I add a class to? any elements
    12. How many times can I use the same class on a page? unlimited
    13. How do I get my link to open in a new tab? target=_blank
    14. What is the alt attribute in the image tag used for? if the image fails then the text will show up with the explanation of the image
    15. How do I reference an id? #
    16. What is the difference between a section and a div? div doesnt convey meaning whereas the section inside is grouped
    17. What is CSS used for? to add body/style to the website
    18. How to we select an element? Example - every h2 on the page. hold shift while highlighting the specified element
    19. What is the difference between a class and an id? - Give me an example of when I might use each one class is used for multiple elements whereas id is used for one. I would use classes if i was making every image have the same characteristics but ID if i wanted just one of those images to be different.
    20. How do we select classes in CSS? .
    21. How do we select a p element with a single class of “human””? .human
    22. What is a parent child selector? When would this be useful? if i wanted to select only the parent or child of an element when adding styling
    23. How do you select all links within a div with the class of sidebar? .sidebar a
    24. What is a pseudo selector? changes what the cursor looks like when hovering over an item/text
    25. What do we use the change the spacing between lines? line-height property
    26. What do we use to change the spacing between letters? letter-spacing property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform tool then uppercase, lowercase, capitalize
    28. How do I add a 1px border around my div that is dotted and black? dotted black 1px;
    29. How do I select everything on the page? * {}
    30. How do I write a comment in CSS? /**/
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm, it deletes the file without warning and it cannot be recovered
    34. Why should I use version control? its best for group projects and then if i mess up then they can just delete my push
    35. How often should I commit to github? every time
    36. What is the command we would use to push our repo up to github? git push -u origin jaden-ficklin
    37. Walk me through Lambda's git flow. first i fork the document and then copy URL and then open gitbash and go the the designated folder for project, git clone (paste), go inside new designated folder, git checkout -b jaden-ficklin, git add ., git commit -m "add comment of push here", git pus -u origin jaden-ficklin. go to github click compare & pull request change who and from and then submit it 

Stretch Questions

    1. What is the difference between an inline element and a block element? an inline element is in a line itself whereas block elements are a type of block
    2. What happens when an element is positioned absolutely? set outside of the documents layout and has its own
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? set the flex item to width: 100%;
    4. Name 3 elements that are diplay block by default,nav,h1,div 2 elements that are display inline by default button, span and 1 element that is display inline-block by default textarea
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? the box model comprises of content, padding, border, and margin. and one alternative is to use display:flex and then you can use the justify-content element.