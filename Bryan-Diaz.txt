https://codepen.io/bdiaz12345/pen/BaKeWaw?editors=1100

1. What is Semantic HTML? it's the elements of the language (HTML) used to support the meaning of webpages and applications.
    2. What is HTML used for? HTML is used for creating the backbone of a web page or application.
    3. What is an attribute and where do we put it? an attribute is what's used after the opening of a tag to identify what the element's behavior will be.
    4. What is the h1 tag used for? How many times should I use it on a page?
	the h1 tag is used to emphasize a "header". It should only be used with important material, usually once, at the top of the page for the most important content.
    5. Name two tags that have required attributes
<a>, <img>
    6. What do we put in the head of our HTML document? In the head, we link the CSS to the HTML and anything else we want to link to the HTML document, as well as "UTF-8" most commonly in the meta tag.
    7. What is an id? an id is an attribute that can be added to an element, for specificity.
    8. What elements can I add an id to? 
	any, but only one of that id maybe used.
    9. How many times can I use the same id on a page? only once.
    10. What is a class? 
a class is another attribute that can be added to elements, usually to describe them and for specificity when it comes to CSS.
    11. What elements can I add a class to? all of them.
    12. How many times can I use the same class on a page? 
	As many times as you want, as long as you don't go crazy.
    13. How do I get my link to open in a new tab?
	_blank
    14. What is the alt attribute in the image tag used for?
	providing information on an image, especially if it can't be displayed. 
    15. How do I reference an id?
	#
    16. What is the difference between a section and a div
	a section is exactly what it sounds like, it's kind of a way to let other programmers know that they are viewing a section, however, a div, which can also contain many elements, blocks different groups of elements up.
    17. What is CSS used for? 
it's used to style HTML
    18. How to we select an element? Example - every h2 on the page
h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
a class can be used multiple times, whereas a certain id can only be used once. If you were doing a section based on cars, you might give each element the class "car". If you wanted to give a Honda an ID, you might give that element the ID "Honda".
    20. How do we select classes in CSS?
with a .
    21. How do we select a p element with a single class of “human””?
.human
    22. What is a parent child selector? When would this be useful? 
it's when you trickle down the chain by going, for example, .header div h2, to get to one specific element.
    23. How do you select all links within a div with the class of sidebar?
.sidebar
    24. What is a pseudo selector?
a selector like h1:hover{} for example, which allows you to manipulate what happens when you actively hover over the element as opposed to changing the original element with a regular selector.
    25. What do we use the change the spacing between lines?
change the height
    26. What do we use to change the spacing between letters?
using the letter-spacing property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
text-transform: uppercase; text-transform: lowercase; text-transform: capitalize;
    28. How do I add a 1px border around my div that is dotted and black?
div {
  border: 1px dotted black;
}
    29. How do I select everything on the page? 
body{}
    30. How do I write a comment in CSS?
/*   */
    31. How do I find out what file I am in, when I am using the command line? 
ls to list what's in the file you're in, but it will simply tell you at the top of where you can type.
    32. Using the command line - how do I see a list of files/folders in my current folder?
ls (haha)
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
rm. It permanently deletes it without a warning.
    34. Why should I use version control? 
It helps multiple people work on one thing separately and decide when to merge progress.
    35. How often should I commit to github?
very often
    36. What is the command we would use to push our repo up to github? 
git push
    37. Walk me through Lambda's git flow. 
Assignment>fork>clone>create a branch>work on assignment>add/commit/push to branch>pull request on github>submit the link on canvas

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?