https://codepen.io/TaylorT22/pen/zYooJOo

1. What is Semantic HTML? is the use of HTML markup to reinforce the semantics, or meaning, of the information in webpages and web applications rather than merely to define its presentation or look.
    2. What is HTML used for? is used to create electronic documents that are displayed on the World Wide Web. 
    3. What is an attribute and where do we put it? dictates certain aspects of an element. you put it inside of <>.
    4. What is the h1 tag used for? How many times should I use it on a page? the h1 tag is used for headlines. you should use the h1 only once but h 1-however many needed.
    5. Name two tags that have required attributes? name and value
    6. What do we put in the head of our HTML document? information such as the page <title>, links to CSS (if you choose to style your HTML content with CSS), links to custom favicons, and other metadata (data about the HTML, such as the author, and important keywords that describe the document.)
    7. What is an id? used to specify a unique id for an HTML element. You cannot have more than one element with the same id in an HTML document.
    8. What elements can I add an id to? you can add a name or an href
    9. How many times can I use the same id on a page? element can't have more than one ID and an ID can't be used more than once in a page.
    10. What is a class? specifies one or more class names for an element. 
    11. What elements can I add a class to? html elements.
    12. How many times can I use the same class on a page? multiple times.
    13. How do I get my link to open in a new tab? right click open link in new tab.
    14. What is the alt attribute in the image tag used for? provides an alternate text for an image.
    15. How do I reference an id? # what ever the text is then {} and fill with text.
    16. What is the difference between a section and a div? section tag defines sections in a document like chapters, headers, footers whereas div tag defines a division in an HTML document.
    17. What is CSS used for? styling.
    18. How do we select an element? Example - every h2 on the page by using the select element.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one/ id starts with # and is followed by a unique name, only one id selector can be attached to an element./ class assigned to and element has it name surrounded by "-" and followed by class name, multiple class selectors can be attached to an element./ you can use an id for a main header and a class for styling.
    20. How do we select classes in CSS? with .class
    21. How do we select a p element with a single class of “human””? <p class="human">
    22. What is a parent child selector? When would this be useful? a parent child selector is used with the greater than sign and is a direct child of a specified element (<div>). ex: If you have one element like a city and then describe that cities name again in another element.
    23. How do you select all links within a div with the class of sidebar? <div class="sidebar"> and the <a href>
    24. What is a pseudo selector? it can style an element when a user mouses over it, style visited and unvisited links differently and style an element when it gets focus
    25. What do we use to change the spacing between lines? line-height
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: UPPERCASE, lowercase or Capitalize
    28. How do I add a 1px border around my div that is dotted and black? border: 1px (color);
    29. How do I select everything on the page? * selector
    30. How do I write a comment in CSS? /* comment here */
    31. How do I find out what file I am in, when I am using the command line? cd (create directory).
    32. Using the command line - how do I see a list of files/folders in my current folder? ls -al
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm, you have to be careful because it can delete all files on your computer and ruin it.
    34. Why should I use version control? collaboration, for storing versions properly, for restoring previous versions, for understanding the changes that were made. 
    35. How often should I commit to github? as many times as you want.
    36. What is the command we would use to push our repo up to github? git push.
    37. Walk me through Lambda's git flow. create your own repo, copy code for repo, cd where you want to store your repo, clone your repo, cd repo name, git checkout -b <firstName-lastName>, code . to pull up your project into visual studio code, then add and commit changes, git push to send to github and then do a pull request.

    Stretch Questions

    1. What is the difference between an inline element and a block element? block elements take up the full width available while the inline elements take the required width to display the contents of the elements.
    2. What happens when an element is positioned absolutely? it is removed entirely from the document flow.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? inline-flex.
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default? display block: <div>, <address>, <h1>-<h6>/ display inline: <span>, <button>/ inline-block: <img>.
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? a box that wraps around every HTML element. It consists of: borders, margins, padding and the actual content. The "fix" for the box model is box-sizing. 