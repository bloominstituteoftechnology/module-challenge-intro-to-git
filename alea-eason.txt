https://codepen.io/SquishieDragon/pen/zYqVLVm?editors=1100

    1. What is Semantic HTML?     Semantic HTML discribes exactly what the element is based on its name 
    2. What is HTML used for? HTML is the meat of a webpage.    It is all the content and structure.
    3. What is an attribute and where do we put it?    attributes are always in the start tag and provide addtional information about the element
    4. What is the h1 tag used for?    How many times should I use it on a page? the H1 tag is uesd to give importance to an element. ONLY USE 1!
    5. Name two tags that have required attributes    <a> and <src>
    6. What do we put in the head of our HTML document?    the title of the page
    7. What is an id?    the id tag gives a name to a string
    8. What elements can I add an id to?    any HTML elements
    9. How many times can I use the same id on a page?    as many as you want provided they have differant names
    10. What is a class?   It is an attribute used to specify a class for an HTML element
    11. What elements can I add a class to?      lots
    12. How many times can I use the same class on a page?  lots
    13. How do I get my link to open in a new tab?   right click it and then click open in new tab
    14. What is the alt attribute in the image tag used for?     used to provide alternet text
    15. How do I reference an id?     #nameofid
    16. What is the difference between a section and a div     section is semantic syntax, div is generic
    17. What is CSS used for?    CSS is the makeup of a webpage. It is used to make the page come alive and add decoration.
    18. How to we select an element? Example - every h2 on the page  use .h2 to select all elements with that tag
    19. What is the difference between a class and an id? - Give me an example of when I might use each one     id is one element, class is more than one. Class can surround multipul elements.
    20. How do we select classes in CSS?     .class
    21. How do we select a p element with a single class of “human””?    <p class="human"> .human to select
    22. What is a parent child selector? When would this be useful?     a <img
    23. How do you select all links within a div with the class of sidebar?    <div class="sidebar">  .sidebar
    24. What is a pseudo selector?   a:hover is an example 
    25. What do we use the change the spacing between lines?     line-height
    26. What do we use to change the spacing between letters?    letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?     text-transform: uppercase; text-transform: lowercase; text-transform: capitalize;
    28. How do I add a 1px border around my div that is dotted and black?    border-style: dotted;     
    29. How do I select everything on the page? * is the universal selector
    30. How do I write a comment in CSS?   /*comment*/
    31. How do I find out what file I am in, when I am using the command line?  ls 
    32. Using the command line - how do I see a list of files/folders in my current folder?    ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?     rmfilename it is permanant 
    34. Why should I use version control?     To be able to revert back should anything break and to let others see your work
    35. How often should I commit to github?  commit often!
    36. What is the command we would use to push our repo up to github?    git push origin branch name
    37. Walk me through Lambda's git flow.  git add > git commit > git push filename

Stretch Questions

    1. What is the difference between an inline element and a block element?   inline element can begin with a line and does not start a new line and only extents as far as it is defined by its tags. Block level elements are used in the HTML body
    2. What happens when an element is positioned absolutely?     it is not dynamic, it will move with the page scrolling.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?      
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 