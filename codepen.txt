https://codepen.io/EscanorZ/pen/NWNWGQO?editors=1100
*Link to codepen*

1. What is Semantic HTML? 
it describes its use clearly to the browser and developer
    2. What is HTML used for? 
    Html is used to build the structure of webpages
    3. What is an attribute and where do we put it? 
    an attribute provides additional information about elements and goes inside elements.
    4. What is the h1 tag used for? 
    How many times should I use it on a page? the h1 tag is used to highlight the most inportant information, it generally is used for titles and should be used sparsely.
    5. Name two tags that have required attributes 
    <a> and <img>
    6. What do we put in the head of our HTML document?
     <header>
    7. What is an id?
     an id works like a variable it gives a name to our elements.
    8. What elements can I add an id to? all elements.
    9. How many times can I use the same id on a page?
     once
    10. What is a class?
     a class is like an id it gives a name to a specific element.
    11. What elements can I add a class to? 
    any
    12. How many times can I use the same class on a page? 
    as many as you would like
    13. How do I get my link to open in a new tab?
     <a target="_blank|_self|_parent|_top|framename">
    14. What is the alt attribute in the image tag used for? 
    if the image cant be displayed it will display this text
    15. How do I reference an id?
     #
    16. What is the difference between a section and a div? 
    a section means that the content is grouped while div doesn't really carry any other meaning to the browser but is for the developer.
    17. What is CSS used for? 
    to style our html
    18. How to we select an element? 
    Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each 
     you want all elements to have same style(class), you want a specific item to have a style(id)
    20. How do we select classes in CSS? 
    .classname
    21. How do we select a p element with a single class of “human””?
     p.human{}
    22. What is a parent child selector? When would this be useful? 
    The parent > child selector in jQuery is used to select all elements that are a direct child of the specified element
    When you know the parent and you have tons of data and then you can just use this to style it easier. 
    23. How do you select all links within a div with the class of sidebar? 
    div.sidebar
    24. What is a pseudo selector?
    A pseudo-class is a selector that selects elements that are in a specific state, 
    they are the first element of their type, or they are being hovered over by the mouse pointer.
    25. What do we use the change the spacing between lines? 
    line-height
    26. What do we use to change the spacing between letters? 
    letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? 
    text-transform(lowercase,capitalize,uppercase)
    28. How do I add a 1px border around my div that is dotted and black? 
    div{
       border-style: dotted;
       border-color: black;
       border-width: 1px;
    }
    29. How do I select everything on the page? 
    *{}
    30. How do I write a comment in CSS? 
    /*
    31. How do I find out what file I am in, when I am using the command line? 
    pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
     ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    rm you don't want to mistakenly delete important data
    34. Why should I use version control? 
    so you can go back to an earlier version if your code breaks
    35. How often should I commit to github? 
    as soon as a new important change is made
    36. What is the command we would use to push our repo up to github? 
    git push
    37. Walk me through Lambda's git flow.
     git add. git commit, git push

Stretch Questions

    1. What is the difference between an inline element and a block element?
     Block elements usually force the line to break wheras inline does not
    2. What happens when an element is positioned absolutely? 
    it positions itself to neareast ancestor or uses document body. 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    display:flex
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    block(<p>, <div>, <li>), inline(<span>, <a>), in-block(<img>)
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
       it is basically what wraps around every element, padding, margins, etc. 
       by using padding

