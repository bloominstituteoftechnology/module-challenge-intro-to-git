https://codepen.io/slothwithannuzzi/pen/GRNrQeq

 1. What is Semantic HTML? 
    2. What is HTML used for? To format webpages.
    3. What is an attribute and where do we put it? An attribute is a special property insterted into the html tag to designate it's behaviour.
    4. What is the h1 tag used for? How many times should I use it on a page? It is used to make the biggest of headlines and can be used as many times as you see fit.
    5. Name two tags that have required attributes: <img> and <a>
    6. What do we put in the head of our HTML document? Links to CSS and metadata
    7. What is an id? a unique element in the html
    8. What elements can I add an id to? any
    9. How many times can I use the same id on a page? once
    10. What is a class? an element that can be used multiple times
    11. What elements can I add a class to? any
    12. How many times can I use the same class on a page? as many times as you want
    13. How do I get my link to open in a new tab?  target=”_blank”
    14. What is the alt attribute in the image tag used for? provides information about the img if it cannot be displayed
    15. How do I reference an id? using '#'
    16. What is the difference between a section and a div? A section defines elements that should be grouped together while a div does not
    17. What is CSS used for? styling the HTML elements
    18. How to we select an element? Example - every h2 on the page: selector
    19. What is the difference between a class and an id? - Give me an example of when I might use each one: a class can be used multiple times while an ID is unique to one specific element.
    20. How do we select classes in CSS? with a period IE .class
    21. How do we select a p element with a single class of “human””? <p class="human">
    22. What is a parent child selector? When would this be useful? unsure
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector? it specifies a condition, for example :hover will change the styling of the elements when you hover over them with your mouse.
    25. What do we use the change the spacing between lines? line-spacing
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform
    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted black;
    29. How do I select everything on the page? ctrl-A
    30. How do I write a comment in CSS? /* */
    31. How do I find out what file I am in, when I am using the command line? ls
    32. Using the command line - how do I see a list of files/folders in my current folder? ls -al
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm
    34. Why should I use version control? 
    35. How often should I commit to github? whenever you finish making changes
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow. fork the github, clone it, make changes, add commit and push, submit link

Stretch Questions

    1. What is the difference between an inline element and a block element? a block element will create a new line when close while an inline will not
    2. What happens when an element is positioned absolutely? the element is fixed to one point and does not move
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? margin
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? the box model is wrapping the entire html in CSS that defines the margins, borders etc of the page