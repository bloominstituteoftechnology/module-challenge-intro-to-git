malcolm-robertson-WEB49
 1. What is Semantic HTML? sections of html
    2. What is HTML used for? adding content to a website.
    3. What is an attribute and where do we put it? it adds a link to css. it goes after the html tag
    4. What is the h1 tag used for? How many times should I use it on a page? once it is the heading of the page.
    5. Name two tags that have required attributes h1,p,div,span,section
    6. What do we put in the head of our HTML document? link to our css style sheet. link to fonts
    7. What is an id? a selector for css
    8. What elements can I add an id to? and html element
    9. How many times can I use the same id on a page? once
    10. What is a class? a selecotor that can be used multiple times.
    11. What elements can I add a class to? any html element
    12. How many times can I use the same class on a page? as many as you want.
    13. How do I get my link to open in a new tab? add an anchor tag
    14. What is the alt attribute in the image tag used for? incase the image doesnt load.
    15. How do I reference an id? #
    16. What is the difference between a section and a div div is box model. section spans the width of the page.
    17. What is CSS used for? styling html
    18. How to we select an element? Example - every h2 on the page h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. an id is more specific and its styles will be used over the class.
    20. How do we select classes in CSS? . property
    21. How do we select a p element with a single class of “human””? .human
    22. What is a parent child selector? When would this be useful? it selects all of the elemnts in the parent or can be used to just select the children.
    23. How do you select all links within a div with the class of sidebar? .sidebar{}
    24. What is a pseudo selector? a selector used to change a certain state of an element.
    25. What do we use the change the spacing between lines? letter-spacing
    26. What do we use to change the spacing between letters? line-height
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform
    28. How do I add a 1px border around my div that is dotted and black? border: solid 1px black; border-decoration: dotted;
    29. How do I select everything on the page? *   {}
    30. How do I write a comment in CSS?    */  //
    31. How do I find out what file I am in, when I am using the command line?  PWD
    32. Using the command line - how do I see a list of files/folders in my current folder? li
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm
    34. Why should I use version control?   It keeps the work you have done organized.
    35. How often should I commit to github?    all the time
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow.
fork it, git clone, work on it, git add, git commit, git push. and send a pull request.
Stretch Questions

    1. What is the difference between an inline element and a block element? inline takes the width of the page.
    2. What happens when an element is positioned absolutely? it is absolute to the whole page not its div.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? add css width property.
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?
