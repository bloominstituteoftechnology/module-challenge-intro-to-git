PART 1 https://codepen.io/ClydeMealy/pen/VwedRWQ
PART 2
    1. What is Semantic HTML? Markup used to provide meaning to the information found in web apps, pages, and browsers.
    2. What is HTML used for? Hypertext Markup Languange is used web pages, hyperlinks, and data entry.
    3. What is an attribute and where do we put it? Is what defines additional charateristics or properties. Always used in the start or opening tag. 
    4. What is the h1 tag used for? Used to define HTML headings.  How many times should I use it on a page? ONLY ONCE!
    5. Name two tags that have required attributes? <a> tag defines a hyperlink and <img>
    6. What do we put in the head of our HTML document? The <head> element. 
    7. What is an id? An attribute with a unique value--used to point to a style--used in JS.
    8. What elements can I add an id to? <h1>, <p>, <script>
    9. How many times can I use the same id on a page? ONLY once in any document.
    10. What is a class? An attribute that specifies one or more classnames for an element.
    11. What elements can I add a class to? In HTML anything from the start tag to the end tag, <h1> Content</h1>. 
    12. How many times can I use the same class on a page? Several times.
    13. How do I get my link to open in a new tab? Press the middle button or scroll wheel on your mouse (if you mouse has the middle click ability) on the link you want to open in a tab. Right click on the link and select open in new tab. New tab will be having the option to 'go back', but it will take you to the page you are on.
    14. What is the alt attribute in the image tag used for? The alt attribute provides alternative information for an image if a user for some reason cannot view it (because of slow connection, an error in the src attribute, or if the user uses a screen reader).
    15. How do I reference an id? Use a hash character (#) then the id name.
    16. What is the difference between a section and a div? Semantics-- the difference of what you are separating.
    17. What is CSS used for? To simplify the process of making web pages presentable.
    18. How do we select an element? Example - every h2 on the page. Depends on what is defined as a second level heading.
    19. What is the difference between a class and an id? The main difference between id and class in CSS is that id is used to apply styling to one unique element while class is used to apply styling to multiple elements. Ex: : #sidebar { border: 1px dashed #cad0d6; margin: 0px 1px 0px 0px; padding: 10px; }, classes using style tags: <style> .green-text { color: #008000; } </style> 
    20. How do we select classes in CSS? Write (.) character followed the name of the class.
    21. How do we select a p element with a single class of “human””? The <p> tag defines a paragraph
    22. What is a parent child selector? Child Selector is used to match all the elements which are child of a specified element. When would this be useful? The element > element selector selects those elements which are the children of specific parent. The operand on the left side of > is the parent and the operand on the right is the children element.
    23. How do you select all links within a div with the class of sidebar? Open navigation pane over the left part of the page content. Open navigation pane over all of the page content. Slide the page content to the right when opening the navigation pane. Display the navigation pane on the right side instead of the left side.
    24. What is a pseudo selector? a keyword added to a selector that lets you style a specific part of the selected element (s).
    25. What do we use the change the spacing between lines? Use the line-height property in CSS.
    26. What do we use to change the spacing between letters? The letter-spacing property increases or decreases the space between.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?The text-transform CSS property specifies how to capitalize an element's text. It can be used to make text appear in all-uppercase or all-lowercase, or with each word capitalized. It also can help improve legibility for ruby. 
    28. How do I add a 1px border around my div that is dotted and black?  border:1px solid black; or (dotted).
    29. How do I select everything on the page? Not sure...
    30. How do I write a comment in CSS? To write CSS comments, you have to add the comment CSS codes or texts inside the start symbol /* and the end symbol */. 
    31. How do I find out what file I am in, when I am using the command line? To do that you use the ls (or list) command. Type ls and press the Return key, and you’ll see the folders (and/or files) in the current directory.
    32. Using the command line - how do I see a list of files/folders in my current folder?
    33. How do I remove a file via the command line? Why do I have to be careful with this? It is permanant!!!!
    34. Why should I use version control? Helps to keep multiple users & versions of code organized.
    35. How often should I commit to github? Satrt early and often!
    36. What is the command we would use to push our repo up to github?  git push -u command
    37. Walk me through Lambda's git flow. Log in to your github account, find assignment, FORK IT, CLONE IT (https version) in BASH, branch, git init,
        git status, , git add, git add ., git push origin or -u command.
Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

