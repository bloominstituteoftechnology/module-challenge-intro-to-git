codepen link: https://codepen.io/danimalcrackrz/pen/PobGMEY

1. What is Semantic HTML?  | describes elements to both browser and developer
    2. What is HTML used for? | structure to a web page
    3. What is an attribute and where do we put it?  | syntax in opening tag to manipulate elements 
    4. What is the h1 tag used for? How many times should I use it on a page? | most important heading, one per page
    5. Name two tags that have required attributes *****
    6. What do we put in the head of our HTML document?  | metadata
    7. What is an id?  | unique element on page
    8. What elements can I add an id to?  | div, anchor, paragraph etc. any html element
    9. How many times can I use the same id on a page?  | once
    10. What is a class?  | class is an attribute which specifies one or more class names for an HTML element
    11. What elements can I add a class to? | any html element
    12. How many times can I use the same class on a page?  | no technical limit
    13. How do I get my link to open in a new tab? | set target attribute to blank
    14. What is the alt attribute in the image tag used for?  | altertanitve text, accessibility such as screen readers
    15. How do I reference an id? | with the '#' symbol and the id 'name'
    16. What is the difference between a section and a div | div is not semantic
    17. What is CSS used for?  | styling 
    18. How to we select an element? Example - every h2 on the page | h2 { }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one | id is unique, perhaps the "container div", 
        and class can be applied to multiple elements,  maybe specific elements that you want styled a certain way. 
    20. How do we select classes in CSS? | dot notation
    21. How do we select a p element with a single class of “human”? | p.human 
    22. What is a parent child selector? When would this be useful? | ">", useful if you wanted to select children of specifc elements
    23. How do you select all links within a div with the class of sidebar? | div > a.sidebar 
    24. What is a pseudo selector? | a:hover
    25. What do we use the change the spacing between lines?  letter-height
    26. What do we use to change the spacing between letters? | letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? | text-transform
    28. How do I add a 1px border around my div that is dotted and black? | border-style
    29. How do I select everything on the page? | '*' 
    30. How do I write a comment in CSS? | /* */
    31. How do I find out what file I am in, when I am using the command line? | pwd 
    32. Using the command line - how do I see a list of files/folders in my current folder? | ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?  | rm -rf , could delete OS
    34. Why should I use version control? | limit scope of changes
    35. How often should I commit to github? | as often as possible
    36. What is the command we would use to push our repo up to github?  | git.push 
    37. Walk me through Lambda's git flow. | fork, clone, branch, add, commit, push 

Stretch Questions

    1. What is the difference between an inline element and a block element? | inline does not start on a new line or take up the full width, 
    while block elements do.
    2. What happens when an element is positioned absolutely? | it scrolls with the page. positioned relative to the nearest positioned ancestor
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? | 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

