   Link to finished codepen   https://codepen.io/christian3ferrer/pen/mdwPboP
   
   
   
   
    1. What is Semantic HTML?
     "HTML that gives meaning to our mark-up".

    2. What is HTML used for? 
    "HTML is used to give web pages structure, such as headings and online documents"

    3. What is an attribute and where do we put it? 
    "Attributes provide additional information about elements and are found in the start tag"

    4. What is the h1 tag used for? How many times should I use it on a page?
     "An h1 tag is our main header and should be used once at the top of our page"

    5. Name two tags that have required attributes 
    "<img> tags and <a> tags"  

    6. What do we put in the head of our HTML document? 
    "Meta data such as links or author name, and title"

    7. What is an id? 
     " specifies a unique id for an HTML element."

    8. What elements can I add an id to? 
     "Any HTML elements"

    9. How many times can I use the same id on a page?
     "once"

    10. What is a class? 
     "attribute specifies one or more classnames for an element."

    11. What elements can I add a class to? 
      "Any HTML elements can have class"

    12. How many times can I use the same class on a page? 
     " There is no limit "

    13. How do I get my link to open in a new tab?
      "target=_blank"

    14. What is the alt attribute in the image tag used for? 
     "Adds a text description to an image "

    15. How do I reference an id?
     " hash character followed by ID name "

    16. What is the difference between a section and a div
     " Sections mean content relates and is grouped while Div conveys no meaning besides its class'

    17. What is CSS used for? 
    " CSS is used to style HTML elements"

    18. How to we select an element? Example - every h2 on the page
    "by typing the specific HTML element "

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
     "ID's can only be used once while a class be used multiple times "

   20. How do we select classes in CSS?
    ".Exampleclass "

    21. How do we select a p element with a single class of “human””? 
    " give it a class name, then    .example p {}  "

    22. What is a parent child selector? When would this be useful? 
    " selects all elements that are a direct child of the specified element,   useful with making lists of if you wanted to single out very specific items"

    23. How do you select all links within a div with the class of sidebar?
    "  .sidebar :link{}"

    24. What is a pseudo selector?
    "selector that specifies a special state of the selected element"

    25. What do we use the change the spacing between lines?
    "line-height property"

    26. What do we use to change the spacing between letters?
    " letter-spacing property"

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    "text-transform"

    28. How do I add a 1px border around my div that is dotted and black?
    "  div {
        border: dotted 1px black;
    }"

    29. How do I select everything on the page? 
    " the "*" selector selects everything on the page"

    30. How do I write a comment in CSS?
    "  /* */   "

    31. How do I find out what file I am in, when I am using the command line? 
    " pwd "

    32. Using the command line - how do I see a list of files/folders in my current folder?
    " ls" 

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    " git rm folder-name "

    34. Why should I use version control? 
    "to keep track of changes and keep all team members working on the latest version "

    35. How often should I commit to github?
    "every 20 minutes "

    36. What is the command we would use to push our repo up to github? 
    "git push -u origin branch-name "

    37. Walk me through Lambda's git flow. 
    " 1. fork our code into our github account
      2. copy our repo link SSH
      3. navigate to our file where we wanna paste our repo link and paste it.
      4. navigate into our new repo file      cd example-repo
      5. create our special branch      git checkout -b "firstname-lastname"
      6. code . so we can open our terminal to start working on the repo
      7. git add .
      8. git commit -m " special discriptive message"
      9. git push -u origin firstname-lastname
      10. submit pull request when done.