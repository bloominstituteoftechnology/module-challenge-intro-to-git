
https://codepen.io/josephjohnston1/pen/pojJNJN?editors=1100


    1. What is Semantic HTML? 
    Using proper markups as intended.
    2. What is HTML used for? 
    Create digital pages to be viewed online.
    3. What is an attribute and where do we put it?
    words used to control an elements behavior. Inside starting element tag.
    4. What is the h1 tag used for? How many times should I use it on a page?
    h1 tag is used to mark the most important title. Should only be used once.
    5. Name two tags that have required attributes
    boolean?
    6. What do we put in the head of our HTML document?
    <head> 
    7. What is an id?
    A special attribute intended for one item. 
    8. What elements can I add an id to? 
    Any
    9. How many times can I use the same id on a page? 
    It should only be used once semantically.
    10. What is a class? 
    A reusable attribute to define an element.
    11. What elements can I add a class to? 
    Any.
    12. How many times can I use the same class on a page? 
    As many as needed.
    13. How do I get my link to open in a new tab?
    add a target="_blank" attribute to your links.
    14. What is the alt attribute in the image tag used for?
    If original link does not work. 
    15. How do I reference an id?
    id=
    16. What is the difference between a section and a div
    section groups like parts. Div has no real meaning.
    17. What is CSS used for? 
    To add style to html pages.
    18. How to we select an element? Example - every h2 on the page
    css electors
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    Both do similiar things. However a class may be used on multiple items while an ID should only be used on one unique item. You can use classes on multiple divs. While an ID you may only use on an individual h1.
    20. How do we select classes in CSS? 
    .classname
    21. How do we select a p element with a single class of “human””?
    p.human {}
    22. What is a parent child selector? When would this be useful? 
    A child refers to an element nested inside of another element, the element it's nested inside of would be the parent. When working on large amounnts of code to save time.
    23. How do you select all links within a div with the class of sidebar? select link children of divs.
    24. What is a pseudo selector?
    Adds additional special attributes to elements.
    25. What do we use the change the spacing between lines?
    Line height.
    26. What do we use to change the spacing between letters?
    letter spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    text-transform: uppercase, lowercase, Capitalize.
    28. How do I add a 1px border around my div that is dotted and black?
    div {
        border: dotted 1px color: black;
    }
    29. How do I select everything on the page?
    * {

    } 
    30. How do I write a comment in CSS?

    //"comment"
    31. How do I find out what file I am in, when I am using the command line?
     pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
    ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    git rm. Does not go to trashcan. Permanently deleted.
    34. Why should I use version control? 
    In case of problems you can easily revert back to older version. Or to work on a new version without effecting current version.
    35. How often should I commit to github?
    20 mins, end of session, or after big changes.
    36. What is the command we would use to push our repo up to github?
    git push [alias] [branch]
    37. Walk me through Lambda's git flow. 
    Go to the project repository
Make sure you signed in to GitHub
Press the “Fork” button. It’s near the top of the repository to the right
Add your TLs as a collaborator. Go to Settings -> Manage Access and click “Invite Collaborator”. You can look up your TLs by their GitHub handles
Double check that you forked correctly. The Repo should say <yourGitHubHandle>/<projectName> near the top. Then click the green “Clone or Download” button and copy the url. 
Open up the terminal (Mac) or GitBash (PC) on your machine. 
Enter the command `git clone <url>` with the url you copied. There is now a local copy of the codebase on your machine. You may enter `ls` and should see the listed. 
Open that folder in your text editor of choice. 
Checkout a new branch using `git checkout -b <firstname-lastname>`
Complete the assignment, adding and committing as needed.
Push your changes to the remote copy using `git push -u origin <firstname-firatname>`
Click “New pull request” or the green “Compare & Pull request” button.
Make sure the base repository is “<yourGitHubHandle>/<projectName>” NOT “LambdaSchool/<projectName>”
Add your TLs as reviewers.
Submit the request

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 