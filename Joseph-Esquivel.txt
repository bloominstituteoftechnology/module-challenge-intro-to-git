	
https://codepen.io/Jayohhh/pen/WNwbOEZ



	1. What is Semantic HTML? 
	1a. a semantic element Cleary describes the meaning to both the browser and the developer
	2. What is HTML used for?
	2a. HTML is used to display plain text on a web page 
	3. What is an attribute and where do we put it?
	3a. Attributes help provide additional information about certain HTML elements and you put them after an normal HTML elements.
	4. What is the h1 tag used for? How many times should I use it on a page?
	4a. The h1 tag is the title of the browser and should only be used 1 time
	5. Name two tags that have required attributes
	5a. The <a> tag which is used to define a hyperlink is paired with the href attribute. The <img> which is used to embed an image in an HTML page it is paired with the src attribute that specifies the path to the image to be displayed
	6. What do we put in the head of our HTML document?
	6a. The title of the document 
	7. What is an id?
	7a. it gives an element a unique identification
	8. What elements can I add an id to? 
	8a. You can add almost any HTML element because it just brings an identity to whatever element it is paired with
	9. How many times can I use the same id on a page? 
	9a. You can never use the same id on a page.
	10. What is a class? 
	10a. The class attribute specifies one or more classnames for an element 
	11. What elements can I add a class to? 
	11a. It can be used with any HTML element 
	12. How many times can I use the same class on a page? 
	12a. You can use the same class multiple times
	13. How do I get my link to open in a new tab?
	13a. You would have to put this code in your html page "<a href='#' target="_blank">#</a>
	14. What is the alt attribute in the image tag used for? 
	14a. It specifies an alternate text for an image
	15. How do I reference an id?
	15a. heres an example of how to reference an id <h1 id="myHeader">My Header</h1>
	16. What is the difference between a section and a div
	16a. A section means that the content inside is grouped and should appear as an entry in an outline of the page. On the other hand a div does not convey any meaning, aside from any found in its title attributes.
	17. What is CSS used for? 
	17a. Css is used for styling a web page 
   	18. How to we select an element? Example - every h2 on the page
	18a. You would type something like this h2 {}
   	19. What is the difference between a class and an id? - Give me an example of when I might use each one
	19a. A class selector is a name preceded by a full stop (".") and a the id is a named preceded by a hash character ("#")
   	20. How do we select classes in CSS?
	20a. To select elements with a specific class, write a period (.) character, followed by the name of the class you would like to select.
   	21. How do we select a p element with a single class of “human””?
	21a. You would write something like this p.class {}
   	22. What is a parent child selector? When would this be useful? 
	22a. A parent child selector helps with changing certain aspects of a section. It you would like all the p elements in a section have the font size of 90px that's when you would use one.
   	23. How do you select all links within a div with the class of sidebar?
	23a. you would use a descendant selector on the links you would to select
   	24. What is a pseudo selector?
	24a. A pseudo selector is a keyword aded to a selector that specifies state of the selected elements.
   	25. What do we use the change the spacing between lines?
	25a. Use the line-height property
   	26. What do we use to change the spacing between letters?
	26a. You would use the letter-spacing property
   	27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
	27a. You would use the text-transform property
   	28. How do I add a 1px border around my div that is dotted and black?
	28a. You would add something like this div {outline-style: dotted; outline-color: Black;} 
   	29. How do I select everything on the page? 
	29a. You would use the * selector
   	30. How do I write a comment in CSS?
	30a. you would write the syntax like this /*your comment*/
   	31. How do I find out what file I am in, when I am using the command line?
	31a. Use the command pwd
	32. Using the command line - how do I see a list of files/folders in my current folder?
	32a. Use the command ls
   	33. How do I remove a file via the command line? Why do I have to be careful with this? 
	33a. You would use rm 'file name' command
   	34. Why should I use version control? 
	34a. Version control is important to keep track of changes and keep every person working on the project off the latest version of the project
   	35. How often should I commit to github?
	35a. You should commit any time you finish a step
   	36. What is the command we would use to push our repo up to github?
	36a. Git push -u origin branch-name 
	37. Walk me through Lambda's git flow. 
	37a. first you are going to want to fork whatever repo is needed to be forked. Next you will add your TL as a collaborator to your forked repo. Next you are going to clone your repo also known as copying the link of your new repo. Next you are going to go to your command line and go to the directory where you are going to want to actually clone your forked repo. Next you'll use the command git clone followed by think link of your forked repo. Next you are going to cd into the newly created repo and create a branch name for the repo using the command git checkout -b. After that you can now work on your repo and start getting projects done. To push changes back up to your GitHub repo you'll use the command git push -u organ branch-name. After the changes are on GitHub you can submit a PR (pull request). 
