link for css + html practice for Part 2 https://codepen.io/jasonours/pen/RwNYeeP

    1. What is Semantic HTML?  Semantic HTML adds meaning to a web page.
    2. What is HTML used for?  HTML is used to add content to a web page. 
    3. What is an attribute and where do we put it?  An attribute provides more information about an element, it is specified at the start of a tag.
    4. What is the h1 tag used for? How many times should I use it on a page?  The h1 tag is used to declare the most important heading. It should only be used once.
    5. Name two tags that have required attributes.  Two tags that have required attributes are the <a> anchor tag that uses the href attirbute and the <img> image tag that uses the src attribute.
    6. What do we put in the head of our HTML document?  The 'head' of the HTML document contains important information like title, keywords, and descriptions.    
    7. What is an id?  id is a CSS selector used on a unique element.
    8. What elements can I add an id to?  id can be added to any element, but only once per page.
    9. How many times can I use the same id on a page?  id can be used once per page.
    10. What is a class?  class is a CSS selector that can be used on multiple elements.
    11. What elements can I add a class to?  class can be added to any element and can be used across multiple elements at the same time.
    12. How many times can I use the same class on a page?  The same class can be added to as many elements as you want.  
    13. How do I get my link to open in a new tab?  To open a link in a new tab, add the target = '_blank' attribute to your link <a href = "www.google.com' target = '_blank'>google.com</a>
    14. What is the alt attribute used for?  The alt attribute is used to specify alternate text if an image doesn't exist or can't display.  
    15. How do I reference an id?  You reference an id using a # and a keyword (#keyword).
    16. What is the difference between a section and a div.  Section usually defines sections in a doc like chapters, headers, or footers (conveys meaning). Div usually defines a division or section and to group block-elements to format in CSS (does not convey meaning).
    17. What is CSS used for?  CSS is used to give style to HTML.
    18. How to we select an element? Example - every h2 on the page. To select an element (like h2), use the element name followed by curly brackets (h2 {}).
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. The difference between class and id is that class can be used to style multiple elements and can be used many times on a page while id only styles one element and is only used once per page. Use id on a single unique element. Use CSS on elements you want to style the same way.
    20. How do we select classes in CSS?  You select a class using a '.' and a keyword (.keyword).
    21. How do we select a p element with a single class of “human””?  To selece a p element with a single class of "human" you would type .human
    22. What is a parent child selector? When would this be useful? A parent child selector selects a specific 'child' within the nested parent. It is useful when you want to have a 'child' underneath the parent styled differently but don't want to add a class or id.
    23. How do you select all links within a div with the class of sidebar?  To select all links in a div with a class of sidebar, use the .sidebar class selector.
    24. What is a pseudo selector?  Apseudo selector is used to define a special state of an element :(hover, :active).
    25. What do we use the change the spacing between lines?  To change line spacing use the line-height property. 
    26. What do we use to change the spacing between letters?  To change the spacing between letters use the letter-spacing property.   
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?  To make everything uppercase use the text-transform: uppercase property, to make everything lowercase use the text-transform: lowercase property, to capitalize first letter use the text-transform: capitalize property.
    28. How do I add a 1px border around my div that is dotted and black?  To make that border style div { border: 1px dotted black}.
    29. How do I select everything on the page?  To select everything on the page use the univeral selector '*'.
    30. How do I write a comment in CSS?  To write a comment in CSS use /* before text and */ after text (/* your text here */).
    31. How do I find out what file I am in, when I am using the command line?  To find out what file you are in type the pwd command in the command line.
    32. Using the command line - how do I see a list of files/folders in my current folder?  To show a list of files, type ls in the command line.
    33. How do I remove a file via the command line? Why do I have to be careful with this?  To remove a file type 'rm filename'. You have to be careful because you will not recieve a prompt to confirm delete... it just deletes. 
    34. Why should I use version control?  Version control keeps track of all modifications made to the code. If a mistake is made, you can easily revert back to a previous working version and fix the mistake. 
    35. How often should I commit to github? Commit everytime you make a change.
    36. What is the command we would use to push our repo up to github? The command we use to push our repo to GitHub is git push origin filename-filename 
    37. Walk me through Lambda's git flow.  1.Fork  2.github repository settings -> add your TL as collaborator  3.git clone into a local file using your terminal  4.in terminal in cloned file create a branch -> git checkout -b "firstname-lastname"  5.make a small change to project -> add -> commit -> git push origin firstname-lastname  6.go back to Github repo, click compare & pull request button  7.click base fork: dropdown -> select firstname-lastname/project-name  8.click reviewers button, add TL as collaborator NOTE: If TL name doesn't pop up when you type it in for reviewer it's because they haven't accepted your collaborator invite. DM them and ask them to accept.  9.click create pull request  10.DO NOT CLICK MERGE PULL REQUEST. TL’s will be the only person to do this, and will only do it after they have confirmed you’ve met MVP.  11.Head back over to your project and start working. add commit push origin firstname-lastname every time you finish a feature.  12.Once MVP is complete make a commit with the commit message “mvp complete.” TL will check.  13.Check your email to see when request is merged
 

Stretch Questions

    1. What is the difference between an inline element and a block element?  Inline element does not start on a new line or stretch to the width of the page. Block element does start on a new line and stretches to fit the width of the page.
    2. What happens when an element is positioned absolutely? When an element is positioned absolutely it positions itself relative to its nearest ancestor.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? Use the box method to add border and padding to the element.
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default. Elements that are block by default are <p>, <div>, and <h1-h6>. Elements that are inline by default are <span> and <a>. An element that is linline-block is <button>.
    5. In your own words, explain the box model. What is the fix for the box model? Box model is a layout style for CSS that adds width, height, margin, border and padding to an element. Using the style box-sizing: border-box makes the layout more manageable .